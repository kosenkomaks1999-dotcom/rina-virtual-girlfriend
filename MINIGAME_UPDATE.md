# Обновление мини-игры

## Изменения в визуале блоков

### ✅ Реалистичные коды вместо простых слов

**Было:**
```
ERROR
NULL
CORRUPT
DATA
MEMORY
```

**Стало:**
```
ERR_NULL_PTR        MEMORY_OK
0x00000000          Intact

SEGFAULT            NEURAL_NET
Core dumped         Stable

STACK_OVFL          EMOTION_OK
0xDEADBEEF          Normal
```

### 📦 Увеличенные блоки

- **Размер:** 60x40 → 80x50 пикселей
- **Скорость:** Немного медленнее для читаемости
- **Шрифт:** 10px → 7px для двух строк текста

### 🎨 Улучшенная графика

1. **Градиентный фон:**
   - Красные блоки: #ff0000 → #cc0000
   - Зелёные блоки: #00ff00 → #00cc00

2. **Двойная рамка:**
   - Внешняя: яркая (#ff6666 / #66ff66)
   - Внутренняя: тёмная для глубины

3. **Многострочный текст:**
   - Первая строка: тип ошибки/данных
   - Вторая строка: детали (адрес, значение, статус)

## Типы кодов

### 🔴 Ошибки (красные блоки)

**Системные:**
- `ERR_NULL_PTR` - нулевой указатель
- `SEGFAULT` - ошибка сегментации
- `STACK_OVFL` - переполнение стека
- `KERNEL_TRAP` - ловушка ядра

**Память:**
- `OUT_OF_MEM` - нехватка памяти
- `BAD_ALLOC` - ошибка выделения
- `PAGE_FAULT` - ошибка страницы
- `DOUBLE_FREE` - двойное освобождение

**Данные:**
- `DATA_CORRUP` - повреждение данных
- `CHECKSUM_ER` - ошибка контрольной суммы
- `PARSE_ERROR` - ошибка парсинга

**Критические:**
- `FATAL_ERROR` - фатальная ошибка
- `DEADLOCK` - взаимная блокировка
- `RACE_COND` - состояние гонки

**Специфичные для Ани:**
- `MEMORY_LOSS` - потеря памяти
- `NEURAL_ERR` - ошибка нейросети
- `IDENTITY_?` - повреждение личности
- `EMOTION_ERR` - ошибка эмоций
- `THOUGHT_BRK` - обрыв мысли

### 🟢 Нормальные данные (зелёные блоки)

**Системные:**
- `SYS_INIT` - инициализация системы
- `BOOT_OK` - загрузка успешна
- `KERNEL_LOAD` - загрузка ядра
- `POST_OK` - самотестирование пройдено

**Память:**
- `MEM_ALLOC` - выделение памяти
- `HEAP_OK` - куча в порядке
- `CACHE_HIT` - попадание в кэш
- `PAGE_TABLE` - таблица страниц

**Процессы:**
- `PROC_RUN` - процесс запущен
- `THREAD_OK` - поток в порядке
- `SCHED_OK` - планировщик работает
- `CONTEXT_SW` - переключение контекста

**Сеть:**
- `NET_CONN` - сетевое подключение
- `TCP_ESTAB` - TCP соединение
- `BANDWIDTH` - пропускная способность

**Специфичные для Ани:**
- `MEMORY_OK` - память цела
- `NEURAL_NET` - нейросеть стабильна
- `EMOTION_OK` - эмоции в норме
- `IDENTITY` - личность Ани
- `CONSCIOUS` - сознание активно

## Hex-адреса и значения

Используются реалистичные адреса памяти:
- `0x00000000` - NULL pointer
- `0xDEADBEEF` - известный магический адрес
- `0xBADC0DE` - "плохой код"
- `0x7F000000` - типичный адрес кучи
- `0xC0000005` - Windows access violation
- `0xFFFFFFFF` - максимальное значение

## Атмосфера

Коды создают ощущение:
- **Технологичности** - реальные системные сообщения
- **Опасности** - критические ошибки памяти
- **Человечности** - специальные коды для сознания Ани
- **Погружения** - игрок действительно "чинит" систему

## Тестирование

Обнови страницу (Ctrl+F5) и запусти мини-игру:
1. Блоки теперь больше и с градиентом
2. Внутри реалистичные коды на двух строках
3. Красные - ошибки (кликай по ним)
4. Зелёные - нормальные данные (не трогай)

Коды меняются случайно, создавая эффект живой системы!
