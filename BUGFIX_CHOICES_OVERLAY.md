# Исправление: Варианты выбора висели поверх всего

## 🐛 Проблема

После пропуска дней при входе в чат варианты выбора оставались видимыми поверх нового контента.

**Причина:** При переходе между экранами (меню → чат) не очищались старые элементы UI:
- Контейнер с выборами (`choicesContainer`)
- Индикатор печати (`typingIndicator`)
- Отступы у чата

## ✅ Решение

Добавлена функция `clearUI()` которая очищает все элементы интерфейса:

```javascript
clearUI() {
    // Скрываем выборы
    const choicesContainer = document.getElementById('choicesContainer');
    choicesContainer.classList.remove('active');
    choicesContainer.innerHTML = '';
    
    // Скрываем индикатор печати
    const typingIndicator = document.getElementById('typingIndicator');
    typingIndicator.classList.remove('active');
    
    // Убираем отступ снизу у чата
    const messagesContainer = document.getElementById('chatMessages');
    messagesContainer.style.paddingBottom = '10px';
    
    // Обновляем позицию кнопки меню
    this.updateMenuButtonPosition();
}
```

## 📍 Где применяется

Функция `clearUI()` вызывается в:

1. **`startNewDay()`** - при входе в чат из меню
2. **`exitToMenu()`** - при выходе из чата в меню
3. **`showMainMenu()`** - при показе главного меню

## 🧪 Тестирование

### Сценарий 1: Пропуск дня
1. Зайди в чат
2. Выйди в меню
3. Нажми "Пропустить день"
4. Зайди в чат снова
5. ✅ Старые выборы не должны висеть

### Сценарий 2: Возврат в тот же день
1. Зайди в чат
2. Выйди в меню (не завершая сеанс)
3. Зайди в чат снова
4. ✅ Старые выборы не должны висеть

### Сценарий 3: Новый день
1. Заверши сеанс (день увеличится)
2. Заработай деньги
3. Зайди в чат нового дня
4. ✅ Чистый интерфейс

## 🔧 Технические детали

**Что очищается:**
- `choicesContainer.classList.remove('active')` - скрывает контейнер
- `choicesContainer.innerHTML = ''` - удаляет все кнопки выбора
- `typingIndicator.classList.remove('active')` - скрывает "печатает..."
- `messagesContainer.style.paddingBottom = '10px'` - убирает отступ
- `updateMenuButtonPosition()` - корректирует позицию кнопки меню

**Порядок вызова:**
```
startNewDay() → clearUI() → hideMainMenu() → startSession()
                    ↓
            Чистый интерфейс!
```

## 📝 Изменённые файлы

- `game.js` - добавлена функция `clearUI()` и её вызовы
- `index.html` - обновлена версия до v=9

## ✨ Результат

Теперь при любом переходе между экранами интерфейс полностью очищается, и старые элементы не висят поверх нового контента.

**Для тестирования:** Обнови страницу (Ctrl+F5) и попробуй пропустить день!
