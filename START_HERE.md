# 🚀 КАК ЗАПУСТИТЬ ИГРУ С VRM

## ✅ ИСПРАВЛЕНО!

Переписал на ES модули - теперь GLTFLoader работает правильно!

## ❌ ПРОБЛЕМА: CORS ошибка

Если открыть `index.html` напрямую (двойной клик), браузер блокирует загрузку файлов из-за политики безопасности CORS.

## ✅ РЕШЕНИЕ: Локальный сервер

### Вариант 1: Python (РЕКОМЕНДУЕТСЯ)

**Если есть Python:**
1. Открой командную строку (cmd) в папке проекта
2. Запусти:
```bash
python -m http.server 8000
```
3. Открой в браузере: `http://localhost:8000`

**Или просто запусти файл:**
- Двойной клик на `start-server.bat`
- Откроется сервер
- Открой `http://localhost:8000` в браузере

### Вариант 2: Node.js

**Если есть Node.js:**
```bash
npx http-server -p 8000
```

### Вариант 3: VS Code

**Если используешь VS Code:**
1. Установи расширение "Live Server"
2. Правый клик на `index.html`
3. Выбери "Open with Live Server"

### Вариант 4: Другие серверы

**PHP:**
```bash
php -S localhost:8000
```

**Ruby:**
```bash
ruby -run -ehttpd . -p8000
```

---

## 🐛 ДРУГИЕ ОШИБКИ В КОНСОЛИ:

### 1. GLTFLoader ошибка
**Исправлено!** Добавлен отдельный импорт GLTFLoader.

### 2. outputEncoding deprecated
**Исправлено!** Заменено на outputColorSpace.

### 3. dialogues.json CORS
**Решается локальным сервером** (см. выше).

### 4. CloudStorage not supported
**Это нормально!** Работает только в Telegram WebApp, в браузере используется localStorage.

---

## 📋 ЧЕКЛИСТ ЗАПУСКА:

- [ ] Файл `qwe.vrm` находится в корне проекта
- [ ] Запущен локальный сервер
- [ ] Открыт `http://localhost:8000` (не file://)
- [ ] Консоль браузера (F12) открыта для отладки
- [ ] Подождал 5 секунд для загрузки VRM

---

## ✅ ЕСЛИ ВСЁ РАБОТАЕТ:

Ты увидишь:
1. ✅ "Загрузка модели..." исчезнет
2. ✅ 3D аватар Ани появится
3. ✅ Она будет моргать
4. ✅ Следить за курсором
5. ✅ Менять выражение лица

---

## 🔧 ЕСЛИ НЕ РАБОТАЕТ:

### Проверь консоль (F12):

**"THREE.GLTFLoader is not a constructor"**
→ Обнови страницу (Ctrl+F5)

**"Failed to fetch qwe.vrm"**
→ Проверь, что файл qwe.vrm в корне проекта

**"CORS policy"**
→ Используй локальный сервер (см. выше)

**Чёрный экран**
→ Подожди 5-10 секунд, модель загружается

---

## 💡 БЫСТРЫЙ СТАРТ:

1. **Запусти:** `start-server.bat`
2. **Открой:** `http://localhost:8000`
3. **Жди:** 5 секунд
4. **Играй!** 🎮

---

Если всё равно не работает - скинь скриншот консоли (F12), разберёмся!
